import logging
from aibenchmark import benchmark
from aibenchmark import metrics
from aibenchmark import dataset

logging.basicConfig(level=logging.INFO, format="%(asctime)s %(levelname)s %(message)s")
